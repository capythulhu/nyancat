/*
 * This algorithm is very simple. Try running `nyancat examples/random_bit`.
 * It generates a random bit from measuring a qubit in superposition.
 */

h 0?        // Hadamards the qubit "0"
m 0?        // Measures the qubit "0"
end [0%]    // Ends the algorithm, outputing the measurement result